#!/bin/bash

choice=$(printf "1) Google Chrome\n2) Brave" | wofi --dmenu --prompt "Choose Browser")

case "$choice" in
    "1) Google Chrome")
        google-chrome-stable & ;;
    "2) Brave")
        brave & ;;
esac
